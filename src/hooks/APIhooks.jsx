
//hookscréer pour faire une demande a un serveur json avec un user a base de searchquery, example chercher des recette sur le site x.com.
/*

import React, {useState, useEffect} from 'react'

import CardDisplay from '../../card/card.display.component'

const UseEffectExample =() => {
    const [user, setUser] = setState(null);
    const [searchQuery, setSearchQuery] = useState('');

    if searchQuery length > 0) {
        useEffect(()=>{
            if (searchQuery.length > 0){
            const fetchFunc = async()=> {
                const response = await fetch(
                    `https://jsonplaceholder.typicode.com/${user}?${username}=${searchQuery}`
                )
                const resJson = await response.json();
                setUser(resJson[o])
            }
            fetchFunc()
           }
    }, [searchQuery])
}
}

*/